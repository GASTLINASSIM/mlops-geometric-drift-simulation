[core]
    remote = minio
['remote "minio"']
    url = s3://mlops-storage/data
    endpointurl = http://localhost:9090
    use_ssl = false
    access_key_id = minio_user
    secret_access_key = minio_password
